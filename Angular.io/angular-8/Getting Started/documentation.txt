/****************** 23-06-2019 *****************/
1. Getting Started with Angular: Your First App
    1.1. Create a new project

    1.2. Template syntax
        Angular's template syntax extends HTML and JavaScript.

        note*** : Structural directives shape or reshape the DOM's structure, typically by adding, removing, and manipulating the elements to which they are attached. Any directive with an * is a structural directive.

        A. Structural Directive ( *ngFor="let product of products" )
        B. Interpolation syntax ( {{}} )
        C. Property binding [] syntax ( [title]="product.name + 'details' ")
        D. (ngIf)


